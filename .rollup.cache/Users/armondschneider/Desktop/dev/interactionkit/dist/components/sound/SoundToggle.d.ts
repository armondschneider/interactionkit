type Props = {
    initialOn?: boolean;
    className?: string;
    playSound?: boolean;
    onToggle?: (on: boolean) => void;
};
export default function SoundToggle({ initialOn, className, playSound, onToggle, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
